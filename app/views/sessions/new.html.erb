<h2 class="ui center medium aligned icon header">
  <i class="circular orange power off icon"></i>
  Rejoignez une communauté de plus de 48 utilisateurs
</h2>
<div class="ui placeholder segment">
  <div class="ui two column very relaxed stackable grid">
    <div class="column">
      <%= form_for(:session, html: {class: "ui form", role: "form"}, url: login_path) do |f| %>
        <div class="field">
          <%= f.label :pseudo, "Pseudo" %>
          <div class="ui left icon input">
            <%= f.text_field :pseudo, placeholder: "Pseudo" %>
            <i class="user icon"></i>
          </div>
        </div>
        <div class="field">
          <%= f.label :password, "Mot de passe" %>
          <div class="ui left icon input">
            <%= f.password_field :password, placeholder: "Mot de passe" %>
            <i class="lock icon"></i>
          </div>
        </div>
        <%= f.button "Se connecter", class: "ui orange submit button" %>
      <% end %>
    </div>
    <div class="middle aligned column">
      <div class="ui big orange button">
        <i class="signup icon"></i>
        S'inscrire
      </div>
    </div>
  </div>
  <div class="ui vertical divider">
    Ou
  </div>
</div>